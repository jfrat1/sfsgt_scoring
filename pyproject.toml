[tool.ruff]
line-length = 120
exclude = [
  "sfsgt_scoring_2023",
  "sandbox",
]

[tool.ruff.lint]
select = ["E", "F", "W", "I"]

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["F401"]

[tool.mypy]
exclude = [
  "sfsgt_scoring_2023",
  "sandbox",
]
